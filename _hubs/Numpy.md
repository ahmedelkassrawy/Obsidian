---
description: "Hub: every note about Numpy"
type: hub
domain: cs
tags:
  - type/hub
  - topic/numpy
---
# Numpy

> [!info] Array basics only, picked up inside the pandas notes. There is no dedicated numpy note yet.
> Part of [[MOC - CS Fundamentals]]. Also try the tag `#topic/numpy`.

## References & cheat sheets
- [[Pandas Notes]] `raw` — Pandas working notes: series vs dataframe indexing, iterating with iterrows, assigning with .loc, and numpy array iteration.

## Course notes
- [[Numpy - Arithmetic Operations]] `raw` — Code-only notes on numpy arithmetic and the useful element-wise operations (add, subtract, multiply, divide).
- [[Numpy - Creating And Accessing Arrays]] `raw` — Code-only notes on creating an ndarray from a list, checking its type, and indexing elements.
- [[Numpy - Data Types]] `raw` — Code-only notes on creating arrays with an explicit dtype (float, int, string) and what casting does to the values.
- [[Numpy - Dimensions Shape And Reshape]] `raw` — Code-only notes on ndim, shape and reshape for 1D, 2D and 3D arrays.
- [[Numpy - Memory Layout And Mixed Types]] `raw` — Code-only notes showing with id() that a numpy array sits contiguously in memory while a list does not, and what happens when types are mixed.
- [[Numpy - Slicing And Memory Use]] `raw` — Course notes on numpy slicing ([start:end:step]) opening with the memory and speed numbers against plain lists.

## Related hubs
[[Pandas]]

## Notes to self (from the audit)
- [[Numpy - Dimensions Shape And Reshape]]: Contains a typo in the pasted code: `prnnt(my_array2.ndim)`.

See also: [[Knowledge Gaps Audit 2026-09-15]] for what is still missing here.
