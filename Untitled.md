Chunking -> if we embed the whole chapter as one vector , the retrieval precision is low , a findable unit and a focused embedding on one topic is better and the third reason is the context budget

metadata -> ones used in search are the chunk text itself, chunk id,which chapter
`id, chapter/filename, chunk_text, embedding`.