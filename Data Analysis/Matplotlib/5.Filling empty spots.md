```python
import pandas as pd
from matplotlib import pyplot as plt

  
data = pd.read_csv("C:\\Users\\Dell\\Downloads\\data.csv")
ages = data['Age']
dev_salaries = data['All_Devs']
py_salaries = data['Python']
js_salaraes = data['JavaScript']

  
plt.plot(ages, dev_salaries, color='#444444',
         linestyle='--', label='All Devs')

plt.plot(ages, py_salaries, label='Python')

overall_median = 57287

plt.fill_between(ages,py_salaries,dev_salaries, alpha =0.25, where=(py_salaries > dev_salaries) , interpolate=True , label="Above Avg")

plt.fill_between(ages,py_salaries,dev_salaries, alpha =0.25, where=(py_salaries <= dev_salaries) , interpolate=True , color="red", label = "Under Avg")

#we pass two axies where this will fill untill the end but if we make 3 arguments like here it will fill depending to the common in between
#alpha = 0.25 which is the transperancy of the color
#where is the condition we need to specifiy the color or the label 
#When `interpolate=True`:

#The function will create a smooth fill between data points.
# It interpolates (estimates intermediate values) between the given data points to create a continuous filled area.
# This is particularly useful when you have gaps in your data or when the lines being filled between have different shapes.

plt.legend()
  
plt.title('Median Salary (USD) by Age')

plt.xlabel('Ages')

plt.ylabel('Median Salary (USD)')

  

plt.tight_layout()


plt.show()
```

![[Pasted image 20240808202951.png]]